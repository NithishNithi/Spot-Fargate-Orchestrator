# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build output directory
bin/

# Built binary (main executable)
spot-fargate-orchestrator

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Log files
*.log

# Environment files
.env
.env.local
.env.*.local

# Temporary files
tmp/
temp/

# Coverage reports
coverage.txt
coverage.html
coverage.out

# Kubernetes config (if accidentally committed)
kubeconfig
*.kubeconfig

# AWS credentials (if accidentally committed)
.aws/
credentials

# Docker build context
# .dockerignore

# Helm charts (if generated)
charts/

# Terraform files (if used for infrastructure)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Local development overrides
config.local.toml
docker-compose.override.yml